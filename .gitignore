# Log / cache do mpv sinh ra khi chạy
*.log
*.cache
cache/

# Trạng thái phát do mpv sinh ra (watch later, lịch sử)
watch_later/
watch-later/
mpv-history.log
history.log

# File tạm của hệ điều hành / editor
.DS_Store
Thumbs.db
*.swp
*~

# Cài đặt cục bộ của http-ytproxy: binary đã build và CA sinh riêng trên từng
# máy, không thuộc về repo cấu hình. Giữ dòng này để `git clean -xdf` trong
# ~/.config/mpv không xoá mất bản cài.
scripts/http-ytproxy/
