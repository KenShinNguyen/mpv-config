# Tên file tạm dùng để trung chuyển qua clipboard.
# LƯU Ý: read_options KHÔNG bóc dấu nháy, đừng bọc giá trị trong ' hoặc ".
name=mpv-screenshot.png

# Kiểu dữ liệu đưa vào clipboard, phải khớp với đuôi của `name`.
# Giá trị phụ thuộc hệ điều hành:
#   Linux (X11, qua xclip): image/png  — Wayland (wl-copy) và Windows bỏ qua field này
#   macOS (osascript):      «class PNGf»
type=image/png
