# Ngôn ngữ nguồn dùng làm phụ đề phụ (secondary) khi chạy Alt+y.
source_lang=en

# Thư mục cache phụ đề đã tải. Mặc định `~~cache/ytsub`, tức thư mục cache của
# mpv (~/.cache/mpv trên Linux/macOS, %LOCALAPPDATA%\mpv\cache trên Windows).
# Hỗ trợ cả `~/...` lẫn `~~/...`; bỏ dấu # để đổi.
#cache_dir=~~cache/ytsub
